The code is written in Python, Pytorch, cuda are requried. 

To generate the fake RGB images, the detailed steps are given below. 

1. Add the NIR images to the folder: /dataset/Validation, please make sure the png images contains "nir" in the filename. Otherwise, please do necssary rectification in /data/VCIP_nir2rgb_dataset.py,  class VCIPNir2RGBDataset_gen(BaseDataset): self.A_paths= [f for f in self.dir_A.glob('*nir*.png') if is_image(f)]

2. In launch.json, please find the "name": "Python: Generate_results", change the "--gpu_ids" to "0" or "1" accordingly. 

3. In vscode or the other alternative IDEs, in the debug mode, choose Python: Generate Results.

4. Go to "/final_results/results/test_latest_iter950/images", the generated RGB files are inside this folder. 

生成虚假RGB图像步骤：
添加NIR图像：

将NIR图像放入文件夹：/dataset/Validation。
确保PNG图像文件名包含 "nir"。如果不包含，请在 /data/VCIP_nir2rgb_dataset.py 中做出必要的更正，修改类 VCIPNir2RGBDataset_gen(BaseDataset): self.A_paths= [f for f in self.dir_A.glob('*nir*.png') if is_image(f)]。
配置GPU ID：

在 launch.json 文件中找到配置，将 "--gpu_ids" 更改为 "0" 或 "1"，具体根据你的需求选择。
在VSCode或其他IDE中运行：

在调试模式下，选择 "Python: Generate Results"。
查看生成的RGB图像：

在路径 "/final_results/results/test_latest_iter950/images" 下可以找到生成的RGB文件